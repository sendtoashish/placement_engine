server:
  port: 9090
spring:
  application:
    name: Config_Server
  profiles:
    active: native
    #active: git
  cloud:
    config:
      server:
        native:
         search-locations: classpath:/configs
        #git:
          #uri: https://github.com/saurabhG2it/SpringCloudCOnfig.git
           #username:
           #password:
          #searchPaths: configs
          #default-label: main
          #timeout: 5
          #clone-on-start: true
          #force-pull: true


